# Mode local (défaut) : pas d'appel API, auth et événements en démo.
# Mettre à true quand le backend est prêt pour utiliser l'API.
# VITE_USE_BACKEND=false

# URL de l'API backend (quand VITE_USE_BACKEND=true)
# VITE_API_URL=http://localhost:3000/api
